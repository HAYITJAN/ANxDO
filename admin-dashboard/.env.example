# Backend API — deployda HTTPS + /api
VITE_API_URL=http://localhost:5000/api

# Asosiy sayt (kinolar havolalari) — deployda frontend URL
VITE_PUBLIC_SITE_URL=http://localhost:3000

# server/.env: ADMIN_CLIENT_URL shu admin panelning deploy URL i bilan bir xil bo‘lsin (CORS).
